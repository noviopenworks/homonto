# Subagent Progress — command-projection
review_mode: standard | tdd_mode: tdd | base: a10061e
Tasks 1-7 DONE (+ prereq 5a12a5d onto declares example-command). Task7 engine keystone risk-reviewed APPROVED (CM3 minor). tasks.md checked 1.1-1.3,2.1-2.5,3.1,3.2,4.1-4.3,5.1-5.3. WithCommandCatalogRoot stubs on both adapters (wired in T8/T9).
Task 8 (claude adapter command projection) NEXT — CRITICAL: managedRoots() must add commandCatalogRoot (non-empty guard); every link.* call passes managedRoots()...; command.<n> state; adopt path uses commandSource.
