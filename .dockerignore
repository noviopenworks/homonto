# Keep the e2e build context lean and hermetic: exclude VCS, local state, the
# checked-in binary, and tooling caches. Source is needed to `go build`, plus
# LICENSE and README.md which the release-packaging suite bundles into archives.
.git
homonto
graphify-out
.graphify_labels.json
.homonto
docs
*.md
!README.md
