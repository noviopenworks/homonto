# Tasks — framework-ecosystem-model (design-only)

## 1. Architecture design
- [ ] Produce the target manifest schema (additive v2), the resolution/validation
      pipeline, capability + compatibility model, local-source trust reuse,
      explicit conflict policy, and a phased MVP→full delivery plan.

## 2. Surface decisions
- [ ] Enumerate the blocking maintainer decisions (D1 local frameworks, D2
      capabilities, D3 conflict policy, D4 F38, D5 first-impl scope) with a
      recommendation for each.

## 3. Stop at design-approval
- [ ] Present the design + decisions for maintainer review before any
      implementation (no code changes in this change).
