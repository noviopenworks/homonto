# Keep the e2e build context lean and hermetic: exclude VCS, local state, the
# checked-in binary, and tooling caches. Only source is needed to `go build`.
.git
homonto
graphify-out
.graphify_labels.json
.homonto
docs
*.md
